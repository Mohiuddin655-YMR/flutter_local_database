import 'package:flutter_test/flutter_test.dart';
import 'package:local_database/database.dart';

void main() {
  test('adds one to input values', () {
    final calculator = LocalDatabase();
    calculator.base;
  });
}
